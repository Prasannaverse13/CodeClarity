
// This file can be used to register Genkit flows for local development and debugging with `genkit start`
// No Genkit flows are directly used by the application if Sensay is the sole provider via API routes.
// If you add Genkit flows later (e.g., for other AI models or tasks), import them here.
// Example: import '@/ai/flows/your-genkit-flow';

console.log("Genkit dev server configuration loaded. No flows registered by default when using Sensay-only mode.");
