# Example .env.local file
# Copy this to .env.local and add your GitHub Personal Access Token
# Create your PAT token by following instructions here: https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens
# Ensure the token has the necessary permissions (e.g., read:user, potentially copilot access if restricted)

GITHUB_TOKEN="ghp_YOUR_GITHUB_PERSONAL_ACCESS_TOKEN"

# Optional: If using Google AI for other flows (though not used in explain-code currently)
# GOOGLE_GENAI_API_KEY="YOUR_GOOGLE_AI_API_KEY"
